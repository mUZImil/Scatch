<!DOCTYPE html>
<html lang="en">
<head>
 <meta charset="UTF-8">
 <meta name="viewport" content="width=device-width, initial-scale=1.0">
 <title>Document</title>
 <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-[#e4f2ff] min-h-screen flex">
  <div class="p-6 min-h-full border-r border-black min-w-[15vw]">
   <div class="flex flex-col gap-3">
     <button class="text-[13px]">New collection</button>
     <button class="text-[13px]">high price</button>
    </div>
  </div>

  <div class="p-6 min-h-full grid grid-cols-3 gap-6 border-l">
    <% allProducts.map((product)=>{ %>
         <div class="p-4 bg-[<%= product.bg %>] flex flex-col justify-between">
            <div class="flex flex-col">
             <img src="<%= product.img %>" alt="image" class="h-[250px] w-[300px]"/>
            </div>

            <div class="border-t border-black p-4 flex justify-between">
              <label> <%= product.name %> </label>
              <label> <%= product.price %> </label>
            </div>
         </div>
    <% }) %>
  </div>
</body>
</html>